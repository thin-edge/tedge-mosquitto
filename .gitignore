# local secrets / cloudsmith token (provided via CI secrets, never committed)
.env

# build outputs (musl -> dist/, gnu -> dist-gnu/)
dist/
dist-gnu/
*.log

# zig build artifacts (build runs inside ./src)
src/cjson/
src/zig-out/
src/.zig-cache/
src/zig-pkg/
